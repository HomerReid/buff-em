TotalPoints=100000;

f=open("/tmp/TOL5.out","w");
Buckets=zeros(8);
for n=1:TotalPoints

 #################################################################
 #################################################################
 #################################################################
 Xi1=rand();
 Xi2=rand();
 if (Xi2>Xi1) continue; end
 Eta1=rand();
 Eta2=rand();
 if (Eta2>Eta1) continue; end

 u1=Eta1-Xi1;
 u2=Eta2-Xi2;

 #################################################################
 #################################################################
 #################################################################
 if     (u1>0.0 && u2>0.0 )
  Q1=1;
 elseif (u1<0.0 && u2>0.0 )
  Q1=2;
 elseif (u1<0.0 && u2<0.0 )
  Q1=3;
 elseif (u1>0.0 && u2<0.0 )
  Q1=4;
 end

 if     (abs(u1) > abs(u2))
  Q2=1;
 else
  Q2=2;
 end

 @printf(f,"%i%i %e %e %e %e %e %e \n",Q1,Q2,u1,u2,Xi1,Xi2,Eta1,Eta2);
 Buckets[ (Q2-1)*4 + (Q1-1) + 1] +=1;

end
close(f);

for Q1=1:4, Q2=1:2
 @printf("%i %i %e \n",Q1,Q2,float(Buckets[ (Q2-1)*4 + (Q1-1) + 1])/float(TotalPoints));
end
